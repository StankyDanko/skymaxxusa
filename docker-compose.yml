services:
  skymaxxusa:
    container_name: skymaxxusa
    image: localhost/skymaxxusa:latest
    ports:
      - "4000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
